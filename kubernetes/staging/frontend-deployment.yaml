kind: Deployment
apiVersion: apps/v1
metadata:
  name: ai-assistant-deploy-frontend-staging
  namespace: ai-assistant-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-assistant-staging-frontend
  template:
    metadata:
      labels:
        app: ai-assistant-staging-frontend
    spec:
      containers:
      - name: frontend-staging
        image: FRONTEND_STRING_TO_BE_REPLACED
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
      imagePullSecrets:
      - name: ai-assistant-ecr
---
apiVersion: v1
kind: Service
metadata:
  name: ai-assistant-service-frontend-staging
  namespace: ai-assistant-staging
  labels:
    app: ai-assistant-staging-frontend
spec:
  type: ClusterIP
  selector:
    app: ai-assistant-staging-frontend
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000